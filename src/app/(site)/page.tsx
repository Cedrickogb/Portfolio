import Hero from '@/app/components/Hero';

/* Le chrome (thème, Navbar, Footer, scanlines, grille) est fourni par
   ThemeProvider dans layout.tsx — cette page ne rend que son contenu. */
export default function HomePage() {
  return <Hero />;
}
